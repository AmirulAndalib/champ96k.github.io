---
interface Props {
  tags?: string[] | undefined;
}
const { tags = [] } = Astro.props;
---

{
  tags.length > 0 && (
    <div class="tags">
      {tags.map((tag) => <span class="tag">{tag}</span>)}
    </div>
  )
}